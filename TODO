Thinking:
    Generic trait system:
        Recursive validation
        Runtime vs compile time polymorphism
        Generated code (Use tracking)
    Compilation:
        Tokenize
        Substitute
        Parse modules
        Rewrite AST
        Validate modules
        Construct tables
        Cross-validate tables
        Convert types/methods
        Generate C code
        Compile C code
    Module Parser implementation:
        AST Node types?
        Maps?
    What is tracked for:
        Compilation Unit
        TopLevelDecl
    Checker Implementation:
        What parts should be checked when?

Coding:
    Testing framework and tests
    Switch to UTF-8
    Split up sections
    ASTNode_##node_type##_destroy(Arena *arena)
    apaz-libc:
	Arena alignment with pointer arithmetic rather than _Alignof(max_align_t)
	Split mutexes from memdebug.h/arena.h off into a new mutex.h
        List reduce()
        String regex match?
        String to/from integral types?
